version: 1
name: mcp-stripe-bi
description: "Stripe Business Intelligence MCP server — MRR, churn, cohorts, LTV, and SaaS revenue analytics. 20 tools."
runtime: typescript
entry: src/index.ts

build:
  install: npm install
  command: npm run build

startCommand:
  type: http
  command: PORT=8080 node dist/index.js
